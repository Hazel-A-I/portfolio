import React from "react";
interface Props {
	src: string;
	title: string;
	description: string;
}
const Projects = ({ src, title, description }: Props) => {
	return <div>Projects</div>;
};

export default Projects;
